#!/bin/bash

# Q) list all the file & dirs

# ls stands for list used for listing the files in current directory
# listing files means -> file and directories also as everything is considered to be files in linux. 
# Linux philosophy: everything is a file. Even hardware devices are represented as files. These files act as an interface to the underlying hardware or kernel resources.

ls -l

