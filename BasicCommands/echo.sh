#!/bin/bash

# the above line is called a shebang which indicates with which engine this script will be executed
# you can check with a command > which bash

# script to print hello world to terminal

echo "Hello World!...:)"

# echo command prints the given input to the terminal. However there is an another use case also like when there are multiple client are connected to a same machine echo will print the message to all the connected terminals usally used in adminstartor to send message connected clients etc use cases you can assume like when admin rebooting system he/she can echo the message saying save and disconnect as we are rebooting the machine ...
